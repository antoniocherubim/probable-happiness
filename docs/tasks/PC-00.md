# PC-00 — Congelar o contrato do Personal Core v2

Status: concluída.

## Objetivo

Impedir expansão acidental de escopo antes da simplificação do runtime.

## Escopo

- adicionar `docs/PERSONAL_CORE_V2.md`;
- tornar esse documento obrigatório no profile do próprio projeto;
- atualizar o README para declarar a reorganização;
- remover do prompt do reviewer requisitos genéricos não presentes na task;
- exigir que findings adjacentes sejam backlog não bloqueante.

## Aceite

- nenhuma mudança no comportamento de execução;
- reviewer não menciona Alembic, PostgreSQL, migrations ou rollback por padrão;
- reviewer recebe explicitamente a política de escopo da task;
- documentação obrigatória aponta para o Personal Core v2;
- testes existentes e checks estáticos passam.

## Fora de escopo

Estado, supervisor, Telegram e compatibilidade de runs serão tratados nas
próximas etapas.

## Evidência

- `bash -n` em `agent-loop`, `run_task.sh` e `review_current.sh`;
- 20 testes focados de orquestração/local-only;
- 315 testes unitários;
- `git diff --check`.
